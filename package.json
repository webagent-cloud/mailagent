{
  "name": "emailagent",
  "version": "1.0.0",
  "private": true,
  "description": "Email agent monorepo",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.0"
  }
}
